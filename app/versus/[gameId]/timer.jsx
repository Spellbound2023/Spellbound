import React from "react";

const Timer = () => {
  const time = initTime;

  function displayTime(time) {
    const seconds = time/1000
    return const;
  }

  setInterval(() => {
    displayTime(time)
  }, 1000);
};

export default Timer;
